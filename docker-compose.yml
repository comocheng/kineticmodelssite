version: "3.7"

services:
  db:
    image: postgres
    env_file: .env
  web:
    build: .
    env_file: .env
    volumes:
      - static:/app/static
      - media:/app/media
    depends_on:
      - db
  nginx:
    build: ./nginx
    volumes:
      - static:/app/static
      - media:/app/media
    ports:
      - "8000:80"
    depends_on:
      - web

volumes:
  static:
  media:
